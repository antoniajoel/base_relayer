# 📋 Следующие шаги для деплоя BaseRelayer

## ✅ Что уже готово

- ✅ Проект создан и структурирован
- ✅ Контракты написаны и компилируются
- ✅ Relayer сервер готов
- ✅ Backend API готов
- ✅ Frontend готов с Base meta tag
- ✅ SDK создан
- ✅ Документация написана
- ✅ Скрипты деплоя созданы
- ✅ GitHub Actions настроены
- ✅ Код закоммичен и запушен в GitHub

## 🚀 Что нужно сделать сейчас

### 1. Подготовить переменные окружения

Вам понадобятся:

#### Для контрактов (`contracts/.env`):
- `PRIVATE_KEY` - приватный ключ кошелька для деплоя (должен иметь Base Sepolia ETH)
- `BASESCAN_API_KEY` - API ключ для верификации (получить на https://basescan.org/apis)

#### Для фронтенда (`frontend/.env.local`):
- `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID` - Project ID из WalletConnect Cloud (https://cloud.walletconnect.com)

### 2. Деплой контрактов на Base Sepolia

```bash
# Создайте .env файл в contracts/
cd contracts
cp .env.example .env
# Отредактируйте .env и добавьте PRIVATE_KEY и BASESCAN_API_KEY

# Вернитесь в корень и запустите деплой
cd ..
./scripts/deploy-sepolia.sh
```

**После деплоя сохраните адреса контрактов!** Они понадобятся для настройки relayer, backend и frontend.

### 3. Обновить конфигурацию после деплоя

После деплоя контрактов обновите `.env` файлы:

#### `relayer/.env`:
```
FORWARDER_ADDRESS=0x... (из деплоя)
RELAYER_HUB_ADDRESS=0x... (из деплоя)
RELAYER_PRIVATE_KEY=ваш_приватный_ключ_для_релейера
```

#### `backend/.env`:
```
FORWARDER_ADDRESS=0x... (из деплоя)
RELAYER_HUB_ADDRESS=0x... (из деплоя)
```

#### `frontend/.env.local`:
```
NEXT_PUBLIC_FORWARDER_ADDRESS=0x... (из деплоя)
NEXT_PUBLIC_RELAYER_HUB_ADDRESS=0x... (из деплоя)
```

### 4. Деплой фронтенда на Vercel

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Перейдите в папку frontend
cd frontend

# Запустите деплой
vercel
```

Следуйте инструкциям, затем:

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект `base-relayer`
3. Перейдите в Settings → Environment Variables
4. Добавьте все переменные из `frontend/.env.local` (с префиксом `NEXT_PUBLIC_`)
5. Передеплойте проект

### 5. Запуск локально (для тестирования)

```bash
# Запустите все сервисы
./scripts/start-dev.sh
```

Или по отдельности в разных терминалах:
- `cd relayer && npm run dev` (порт 3002)
- `cd backend && npm run dev` (порт 3001)
- `cd frontend && npm run dev` (порт 3000)

## 📝 Чек-лист

- [ ] Получен Basescan API Key
- [ ] Получен WalletConnect Project ID
- [ ] Создан `.env` в `contracts/` с PRIVATE_KEY и BASESCAN_API_KEY
- [ ] Контракты задеплоены на Base Sepolia
- [ ] Адреса контрактов сохранены
- [ ] Обновлены `.env` файлы в relayer, backend, frontend
- [ ] Frontend задеплоен на Vercel
- [ ] Переменные окружения добавлены в Vercel
- [ ] Проект протестирован локально
- [ ] README обновлен со ссылками на деплой

## 🔗 Полезные ссылки

- [Base Sepolia Faucet](https://www.coinbase.com/faucets/base-ethereum-goerli-faucet) - получить тестовый ETH
- [Basescan API](https://basescan.org/apis) - получить API ключ
- [WalletConnect Cloud](https://cloud.walletconnect.com) - получить Project ID
- [Vercel Dashboard](https://vercel.com/dashboard) - управление деплоем
- [Base Explorer](https://basescan.org) - просмотр транзакций

## 📚 Документация

- [QUICK_START.md](./QUICK_START.md) - быстрый старт
- [DEPLOYMENT.md](./DEPLOYMENT.md) - подробное руководство по деплою
- [docs/api.md](./docs/api.md) - документация API
- [docs/integrations.md](./docs/integrations.md) - интеграция в ваш dApp

## ⚠️ Важно

1. **Не коммитьте `.env` файлы** - они уже в `.gitignore`
2. **Храните приватные ключи в безопасности**
3. **Тестируйте на Sepolia перед Mainnet**
4. **Проверяйте баланс кошелька** перед деплоем

---

**Удачи с деплоем! 🚀**

